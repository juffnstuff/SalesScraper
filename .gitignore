node_modules/
.env
# config/users.json — now tracked (only contains bcrypt hashes, not plaintext)
logs/runs/*.json
# config/icps/*.json  — tracking ICPs in git for visibility
*.log
.DS_Store

# ICPs.md is a generated artifact (run `node scripts/export_icps.js` to
# regenerate). Tracked in the repo as a readable snapshot — re-run after
# ICPs refresh on their 30-day cache cycle to keep it current.
