node_modules/
.env
# config/users.json — now tracked (only contains bcrypt hashes, not plaintext)
logs/runs/*.json
# config/icps/*.json  — tracking ICPs in git for visibility
*.log
.DS_Store

# Generated ICP markdown export. Regenerate any time with:
#   node scripts/export_icps.js
# Remove this line if you want to track a snapshot in the repo.
ICPs.md
